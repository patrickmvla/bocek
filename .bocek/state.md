## Current state
- **Mode:** implementation
- **Feature:** _shared (G + H + #4 batched implementation)
- **Last resolved:** G1' + H1' + #4-1 + #4-2 landed
- **In progress:** alignment work queued from [[research-subfolder]]:
  (R1) extend `scripts/preflight.sh` — count `.research/` entries per feature in vault-state output, extend recent-entries search to depth 3 for `*/.research/*.md` paths;
  (R2) add `bocek vault organize --migrate-research` subcommand to `scripts/bocek` — walks feature folders, identifies `type: research` entries via frontmatter, `git mv`'s them into `.research/`;
  (R3) amend `references/shared/vault-format.md` path-convention section to document the `.research/` extension prominently, beside the parent rule;
  (R4) extend `scripts/enforce-mode.sh` to reject writes to `vault/{feature}/*-research.md` (flat pattern) AND writes to `vault/{feature}/{slug}.md` where frontmatter declares `type: research`.
  Plus, still owed from [[mandatory-feature-folders]] era:
  (P2) move Path convention to the top of `references/shared/vault-format.md`;
  (P3) prime each primitive's "On activation" with a one-line path-convention reminder;
  (P4) tighten `scripts/enforce-mode.sh` to block flat-vault writes structurally.
  Item P1 ("mark ADR-0003 as Superseded") is dropped — ADR-0003 is committed-deleted in working tree; `[[mandatory-feature-folders]]` body still references it as superseded which is a stale pointer to fix as housekeeping, not a decision.
- **Next:** Five further polishes are queued for follow-up `/design` sessions, in this order:
  (#6) lazy-compile becomes a script in `scripts/bocek` (not a prose instruction in `primitives/implementation.md`) — bokchoy evidence: `/implementation` ran multiple times, never produced `.bocek/vault/.compiled/` because the lazy-compile step is markdown the model skips;
  (#4) `index.md` regeneration from frontmatter `description:` field (bokchoy evidence: 113-line index with paragraph-length summary lines violating spec);
  (#5) `state.md` bounded format — explicit rotate or trim past size threshold (bokchoy evidence: 20,353-byte state.md; preflight only loads `head -8`, so ~99% is dead context);
  (#2) `_shared/` misclassification gate — implement the mitigation already specified in [[mandatory-feature-folders]] (bokchoy evidence: 9 of 16 `_shared/` entries are misclassified, 6 with single-feature frontmatter that should live in named folders);
  (#? generic-meta-feature ban — defer until ranking confirms it's still relevant after #2 lands).
  Then implementation seat picks up R1-R4 + P2-P4. R4 + P4 are the load-bearing ones — without them this decision is doctrine without enforcement, exactly the failure pattern the parent corrects.

## Session history
- 2026-05-02 — Diagnosed `scripts/preflight.sh` path-doubling and stale-mode bugs. Fix landed: walk-up `find_project_root` with cruft-skip + read-back verification on mode write. Repo source and install copy synced.
- 2026-05-02 — Discovered doctrinal contradiction between ADR-0003 (feature folders optional) and vault-format.md / primitives / preflight (feature folders mandatory). Classified as design gap, not implementation bug.
- 2026-05-02 — Onboarded bocek-the-project to its own vault (Path A: forward-vault only). First entry: [[mandatory-feature-folders]], superseding ADR-0003 at the path layer.
- 2026-05-14 — `docs/` tree removed from working tree (uncommitted): all 23 historical ADRs + design docs deleted. Human position: outgrew ADRs, moved to vault-style; tree deletion makes [[mandatory-feature-folders]]'s supersedes pointer stale, follow-up housekeeping owed.
- 2026-05-14 — `/review` session against bokchoy (`~/audhd/bokchoy/.bocek/vault/`) revealed six vault-scale failure modes: 89 entries / 58% research, no `.compiled/`, `architecture/` as junk-drawer (39 entries), `_shared/` misclassification (9 of 16), index.md spec violation (paragraph-length summaries), state.md spec violation (20KB, head-8-only-loads). Cross-pattern: bocek's enforcement layer stops at `enforce-mode.sh`; everything else (path convention details, index format, state.md scope, lazy-compile) is prose the model drifts on at scale.
- 2026-05-14 — Researched `mattpocock/skills` (81k stars) as evidence input. Direction locked: steal pieces, keep bocek's modal-enforcement architecture (not matt's composable-skills model — that would break the PreToolUse hook moat).
- 2026-05-14 — `/design` session vaulted [[research-subfolder]] addressing failure mode #3 (research-paired-decision file inflation; bokchoy 52/89 research-type entries). Position B (`.research/` subfolder) over A (fold inline / matt's pattern) and (d) (inverted hierarchy). Self-attack found preflight depth-2 issue; preflight-extension mitigation added pre-vault.
- 2026-05-14 — Same session vaulted [[mattpocock-skills-survey]] and [[vault-scale-audit-bokchoy]] under `_shared/.research/` — dogfooding the new path convention immediately. Both research entries are `provisional: true` pending contradiction-probe channels (BMAD/GSD/Spec-Kit comparison for matt; second-project audit for bokchoy). The decision entry's `related:` was amended to wikilink both; index.md updated.
- 2026-05-14 — `/debugging` session traced bokchoy's absent `.compiled/` directory (audit F7). Root cause: the implementation primitive's "compile" instruction (at `implementation.md:25`, `implementation.md:118`, `contract-following.md:8`) uses only input-direction phrasing ("compile from X"); no write-target verb exists anywhere in the activation flow. There is no instruction for the model to skip or follow — the load-bearing instruction is *absent*, not *ignored*. Sharpened F7 from inferred-medium to production-cited-high. Vaulted [[discovery-compile-instruction-lacks-output-path]] capturing the trace + evidence + two resolution options. The preflight on this turn ALSO reproduced [[research-subfolder]]'s self-attack-predicted depth-2 issue: recent-entries listing didn't include the two new `.research/` files. R1 confirmed observable in the live tool.
- 2026-05-14 — `/design` session opened to resolve Option 2 vs Option 3 for the compile fix. Identified the load-bearing sub-question: does `.compiled/` actually pay rent? Refused to vault a decision without evidence. Handed to `/research` with falsifiable question.
- 2026-05-14 — `/research` session vaulted [[compile-step-rent-research]] under `_shared/.research/`. Triangulated across (a) bokchoy first-principles token math (architecture/ concatenated = 85% of Claude's context window — flat-cat dominated), (b) Aider's repo-map docs (production-cited rejection of flat-cat in favor of PageRank selection), (c) Anthropic prompt-caching docs (model layer auto-provides the re-read benefit), (d) GitHub Blog + Augment Code on read-on-demand (70-95% savings vs. load-everything). Contradiction probe found no credible engineering-grade support for flat-concatenation. Conclusion: **Option 3 (remove `.compiled/`) has stronger evidence than Option 2 (script-driven compile).** F5 names a narrow regime (curated-synthesis + multi-session-per-feature + parsing-cost-tolerance) where Option 2 could pay rent; conditions not verified for actual bocek users. Handing back to `/design` to weigh and choose with the grounded evidence.
- 2026-05-14 — `/design` session vaulted [[compile-removal]] committing to Option 3. Self-attack identified the closed-world Claude assumption as the load-bearing failure mode; mitigated by explicit Revisit-when triggers (bocek-widens-beyond-Claude / observed-F5-workload / Anthropic-cache-deprecation). Polish #6 from [[vault-scale-audit-bokchoy]]'s queue resolved by removal rather than by implementation. Six implementation items now queued under the new polish #6 banner (replacing the original "build compile script"): I1 amend `primitives/implementation.md:25`, I2 amend `primitives/implementation.md:118`, I3 amend `references/implementation/contract-following.md:8`, I4 amend `references/shared/vault-format.md` path-convention layout, I5 audit + clean `.gitignore` in bocek repo + bootstrap template, I6 verify `scripts/bocek` `cmd_status` has no `.compiled/` references.
- 2026-05-14 — `/research` deep-read pass on [[mattpocock-skills-survey]]. Read remaining 13 SKILL.md (all `misc/*`, `personal/*`, `in-progress/writing-*`, `deprecated/*`) + 8 Tier-A supporting files (`improve-codebase-architecture/{DEEPENING,INTERFACE-DESIGN,LANGUAGE}.md` + `tdd/{deep-modules,interface-design,mocking,refactoring,tests}.md`). **F8 corrected** — matt's `misc/git-guardrails-claude-code` IS a PreToolUse hook; the moat is mode-mutex scope, not enforcement-primitive existence. **5 new findings F9-F13:** architectural vocabulary in LANGUAGE.md (refines Ousterhout), parallel-sub-agents pattern shared by deprecated+active interface-design skills, one-adapter-hypothetical seam discipline + 4-category dependency taxonomy, TDD reference layer 5x richer than v1 captured (mock-at-boundaries-only, SDK-style-over-generic-fetcher, etc.), `personal/obsidian-vault/SKILL.md` confirms matt's flat-vault is the architecture bocek explicitly outgrew (triangulates user's earlier defense). **3 new takes H/I/J:** architectural-vocabulary mental-model, parallel-sub-agents position derivation, seam discipline. Take-list re-ranked: priority order now A→H→G→E→J→C→D→B→F→I. Survey stays `provisional: true` — contradiction-probe channel (BMAD/GSD/Spec-Kit) still empty; deep-read thickens matt-side, doesn't close the probe.
- 2026-05-14 — `/design` session vaulted [[context-md-as-vocabulary]] resolving Take A. Six sub-decisions resolved: (1) location = `.bocek/vault/CONTEXT.md` peer of `index.md`, (2) reading discipline = eager every primitive activation, (3) format = matt's CONTEXT-FORMAT + frontmatter + optional `→ see [[wikilink]]`, (4) update discipline = inline during conversation per matt's grill-with-docs rhythm, (5) initialization = bootstrap scaffolds empty stub, (6) multi-context = single-context for v1; CONTEXT-MAP deferred. Sub-decision 7 (feature-folder-name enforcement) explicitly deferred to follow-up `/design` session — captured as D1 in queue. Five implementation items C1–C5 queued.
- 2026-05-14 — `/implementation` seat opened. Landed G8 + G1 (3 contracts: I5 + I6 + C4). `.gitignore` no longer ignores `.bocek/vault/.compiled/`; `scripts/bocek` cmd_status no longer filters `.compiled` from entry counts; new reference `references/shared/context-format.md` documents the CONTEXT.md format per matt + bocek refinements (frontmatter + `→ see [[wikilink]]`). Verified post-edit: `grep "compiled"` of both G8-scoped files returns no matches. Two unvetted-but-justified cleanups: (1) dropped the stale `(ADR-0023)` parenthetical from .gitignore comment because docs/decisions/0023-* is committed-deleted; (2) cleaned the `.bocek/mode` file directly after preflight SIGPIPE killed the script's mode-write (workaround for the bug captured as discovery-owed above).
- 2026-05-14 — G2 landed (4 contracts: P2 + I4 + C5 + R3). `references/shared/vault-format.md` restructured: Path convention moved to the first section after the title (was line 119, now line 5); `.compiled/` removed from path-convention layout; `CONTEXT.md` added as top-level vault meta with cross-reference to [[context-md-as-vocabulary]]; `.research/` subfolder pattern documented in layout (twice: under feature folders AND under `_shared/`) plus a prominent path-convention paragraph cross-referencing [[research-subfolder]]. Verified: 0 occurrences of `compiled` in the file; 5 occurrences of `CONTEXT.md`; 5 occurrences of `.research`. **Discipline note:** initial draft contained three prescriptive over-reach lines (index-must-be-one-line, state-md-head-8 explanation, generic-meta-feature-name anti-pattern). All three correspond to polish items #4/#5/#6-meta-ban that are explicitly queued for future `/design` sessions and not yet vaulted as decisions. Removed pre-finalize per anti-improvisation rule. Pattern observed: synthesizing from [[vault-scale-audit-bokchoy]] findings into prose prescriptions during a structural edit overstepped the contract; will check more carefully on subsequent edits.
- 2026-05-14 — G3 landed (2 contracts: R1 + C1). `scripts/preflight.sh` edits: (1) entry-count line removed `.compiled` filter, added `CONTEXT.md` exclusion (so it doesn't count toward entries); (2) added CONTEXT.md presence check in vault-state output with the new "no CONTEXT.md — create one..." message; (3) feature-folder listing now prints `(N; +M in .research)` per folder via a `while` loop counting depth-2 and depth-3-under-.research/; (4) recent-entries `find` extended to `-maxdepth 3` so `.research/*.md` paths surface in the orientation block, fixing the self-attack-predicted depth-2 bug from `[[research-subfolder]]`; (5) added a conditional CONTEXT.md line to the eager-references block (after the case statement) that fires for all modes except `idle` and only when `CONTEXT.md` exists. Working-tree smoke test passes: `_shared/ (5; +3 in .research)` printed correctly; the two `.research/*-research.md` files now appear in recent-entries; "no CONTEXT.md" message fires correctly (CONTEXT.md doesn't yet exist in bocek's own vault).
- 2026-05-14 — G5 + G6 landed (5 contracts: P3 + C2 + I1 + I2 + I3). All 6 primitives received the path-convention reminder paragraph (cross-referencing the three vaulted path decisions) and a new step 4 for reading `CONTEXT.md` after `index.md` + `state.md`. `implementation.md`'s compile-related framing at lines 25 (now in new step 5) and 118 replaced with direct vault-read framing per `[[compile-removal]]`. `references/implementation/contract-following.md:8` likewise updated. Verified: 0 `compiled` references remain in `primitives/` or `references/`.
- 2026-05-14 — G4 landed (2 contracts: P4 + R4-path-shape; R4 frontmatter-type-check deferred). `scripts/enforce-mode.sh` restructured: added mode-independent `check_vault_path` function BEFORE the code-mode early-exit so vault path conventions apply in all modes (per `[[mandatory-feature-folders]]`'s "regardless of mode" wording). P4 rejects flat-vault writes except `index.md` + `CONTEXT.md`. R4 rejects `vault/{feature}/*-research.md` flat-pattern writes, suggesting the correct `.research/{slug}.md` target. Also extended Bash-tool handling to extract candidate vault paths from commands and run `check_vault_path` on each (heuristic — doesn't handle quoted paths with embedded spaces perfectly). All 6 path-shape smoke tests pass: flat-vault → DENY, index/CONTEXT root → PASS, flat-research → DENY with redirect, `.research/` housed → PASS, regular feature entry → PASS.
- 2026-05-14 — R4 frontmatter-type-check landed (1 contract: R4-frontmatter — the deferred half of R4). Added `check_frontmatter_type_research` function to `scripts/enforce-mode.sh`. Called from Edit/Write handler after `check_vault_path`. For Write: parses `tool_input.content` JSON and awk's the first frontmatter block for `^type:`. For Edit: reads on-disk file at FILE_PATH. For Bash: skipped intentionally (content not available from JSON; heredocs/redirects unparseable). Skip-conditions: path already in `.research/`, or basename is `index.md`/`CONTEXT.md` (top-level vault meta). 4 new smoke tests pass (write+type:research+non-.research → DENY; write+type:decision → PASS; write+type:research+inside .research/ → PASS; CONTEXT.md write with any content → PASS); plus Test 8 isolates the frontmatter check from the path-shape check by using a non-`-research`-suffixed filename, confirms the new function fires correctly. R4 contract fully closed. TODO comment removed from script.
- 2026-05-14 — `/design` session vaulted [[context-md-folder-name-enforcement]] resolving D1 (sub-decision 7 deferred from [[context-md-as-vocabulary]]). Option B (boundary) committed over A (strict; rejected for mid-rename friction without protection benefit), C (soft; **dominated** — doctrine-without-enforcement pattern rejected 4 times in this session), D (none; defeats CONTEXT.md's purpose). Three sub-decisions settled: (i) exact slugified-match between folder name and `**Term:**` header; (ii) "new folder" detected by parent-directory absence on disk; (iii) `_shared/`, `.research/`, `index.md`, `CONTEXT.md` exempt. Queues E1 (check_feature_folder_match function in enforce-mode.sh), E2 (extend design primitive's path-convention reminder to mention the gate), E3 (smoke tests across ≥6 scenarios). Failure mode: bootstrap chicken-and-egg on first-ever feature folder write — mitigated by denial message that names the missing term and target file explicitly, making the fix one Edit away.
- 2026-05-14 — E1+E2+E3 landed (3 contracts). E1: new `check_feature_folder_match` function in `scripts/enforce-mode.sh` called from Edit/Write handler AND from Bash extracted-paths loop. Logic: skip if not under `.bocek/vault/`, skip top-level meta (index.md, CONTEXT.md), skip `_shared/`, compute feature-folder path from rel-path, allow if dir exists (grandfathered), else grep CONTEXT.md for `**Term:**` headers, slugify match (lowercase + collapse `[ -]+` to `-`), deny with templated message naming the suggested TitleCase term + reference to context-format.md if no match. E2: extended all 6 primitives' path-convention reminder paragraph with the new gate sentence + cross-reference to `[[context-md-folder-name-enforcement]]`. E3: 8 smoke-test scenarios all pass — new+missing-term → DENY; new+matching-term (slug-variant) → PASS; existing folder grandfathered → PASS; `_shared/` → PASS; `.research/` (parent exists) → PASS; bootstrap-empty-CONTEXT → DENY with template; no-CONTEXT.md → DENY with bootstrap hint; CONTEXT.md write itself → PASS.
- 2026-05-14 — `/design` session batched three tactical decisions (Take G + Take H + Polish #4 per `[[mattpocock-skills-survey]]` re-ranked list and `[[vault-scale-audit-bokchoy]]` polish queue). Vaulted as three separate entries (one-decision-per-entry per the bocek pattern): **[[lazy-vault-write]]** (matt-take G — one-sentence amendment to design primitive's vault-writes); **[[software-design-vocabulary]]** (matt-take H — new `mental-models/software-design.md` adopting matt's LANGUAGE.md vocabulary wholesale); **[[index-regen-from-frontmatter]]** (bokchoy polish #4 — `bocek vault index --rebuild` script). Implementation queue: **G1'** (one-sentence amendment), **H1'** (~80-line mental-model file), **#4-1** (cmd_vault_index_rebuild function), **#4-2** (smoke test against bocek's own vault).
- 2026-05-14 — G1' + H1' + #4-1 + #4-2 landed (4 implementation contracts). G1': inserted lazy-doc sentence as bolded lead-in of `primitives/design.md` Vault-writes section. H1': new `mental-models/software-design.md` (~70 lines) with matt's 8 terms + Principles + Relationships + Rejected framings + bocek-scope-note + bocek-side cross-references (empty for now) + provenance citing Ousterhout/Feathers/Evans. #4-1: new `cmd_vault_index_rebuild` function in `scripts/bocek` with `--rebuild`/`--check` flags; dispatch routing `bocek vault index ...` added. Logic: extracts project name from CONTEXT.md H1, walks `_shared/` first then other feature folders alphabetically, per-entry resolution order frontmatter `description:` → first H1 → slug-as-title-case fallback, warns on count of entries missing description. #4-2: smoke test against bocek's own 14-entry vault produces a complete spec-conforming index (one line per entry, `[[wikilink]] — type: T — summary` shape), 14 warnings (all existing entries lack description: frontmatter; H1 fallback handles them cleanly). Unknown flag rejected correctly. Vault-format.md amendment to add `description:` as recommended frontmatter field flagged as polish #4-followup, separate decision deferred.
- 2026-05-14 — S1 landed (1 contract from `[[preflight-sigpipe-fix]]`). `scripts/preflight.sh` restructured: new section 0 (capture `$PREVIOUS_MODE`, write `$MODE`, read-back verify) runs immediately after `find_project_root` resolution, before any echo to stdout. Old section 7 (lines 310-319) removed. Old section 1 (mode transition display) updated to use captured `$PREVIOUS_MODE` since the file content is now overwritten before display. Closing banner at line 322 kept as informational. Regression test passes: `echo idle > .bocek/mode && bash scripts/preflight.sh review 2>&1 | head -8 > /dev/null && cat .bocek/mode` now reports `review` (was `idle` pre-fix). The very preflight invocation at the top of this implementation seat was dogfooded (piped through `| head -8`) and mode file correctly updated to `implementation`.
- 2026-05-14 — G7 landed (2 contracts: R2 + C3). `scripts/bocek` extended: cmd_bootstrap now scaffolds `.bocek/vault/CONTEXT.md` with frontmatter and format-example-as-comment stub between vault-index and state.md initialization. cmd_vault_organize accepts an optional `--migrate-research` flag; with no flag, runs existing loose-entry migration (extended to also exempt `CONTEXT.md` from "loose" classification); with `--migrate-research`, routes to new `cmd_vault_organize_research` function which walks each feature folder, finds depth-2 `type: research` entries via frontmatter, and `git mv`'s them into `.research/` subfolders. Dispatch updated to pass `${3:-}` through to `cmd_vault_organize`. Smoke tests pass: unknown flag rejected, known flag accepted and runs the new function (0 entries to migrate in bocek's own vault — already-clean).
- **All 18 implementation items landed.** Group summary:
  - G8: `.gitignore` + `scripts/bocek` cmd_status — `.compiled/` references removed.
  - G1: new `references/shared/context-format.md` documenting matt's CONTEXT-FORMAT + bocek refinements.
  - G2: `references/shared/vault-format.md` restructured (Path convention moved to top, `.compiled/` removed, `CONTEXT.md` and `.research/` added to layout + prose).
  - G3: `scripts/preflight.sh` — `.research/` per-feature counts in vault state output, recent-entries search extended to depth 3, CONTEXT.md presence check + eager-refs entry.
  - G5 + G6: all 6 primitives' *On activation* sections — path-convention reminder paragraph + new step 4 reading `CONTEXT.md` + implementation.md's compile language replaced with direct vault reads.
  - G4: `scripts/enforce-mode.sh` — P4 flat-vault rejection + R4 flat-research-path-shape rejection, both mode-independent. All 6 smoke tests pass.
  - G7: `scripts/bocek` — cmd_bootstrap now scaffolds `CONTEXT.md` (with format-example stub), cmd_vault_organize accepts `--migrate-research` flag routing to new `cmd_vault_organize_research` function.
- **Two follow-ups owed:**
  1. ~~R4 frontmatter-type-check~~ — **RESOLVED.** Implemented in `scripts/enforce-mode.sh` via new `check_frontmatter_type_research` function called from the Edit/Write handler. For Write tool: parses `tool_input.content` JSON for frontmatter type. For Edit tool: reads on-disk file's frontmatter. For Bash tool: skipped intentionally (content not available from JSON; heredocs/multi-step redirects unparseable from a single command string). All 9 smoke tests pass; the previously-deferred TODO comment removed.
  2. **Preflight SIGPIPE — fully closed.** Discovery, decision, AND implementation all landed. `[[preflight-sigpipe-fix]]` Option 1 implemented in `scripts/preflight.sh`: new section 0 (capture PREVIOUS_MODE, write current MODE, read-back verify) runs before any echo; old section 7 removed; closing banner kept as informational. PREVIOUS_MODE plumbing restructured to preserve existing mode-transition display under the new write-ordering (forced implementation detail — flagged at execution time, not architectural improvisation). **Regression test passes:** `echo idle > .bocek/mode && bash scripts/preflight.sh review 2>&1 | head -8 > /dev/null && cat .bocek/mode` now reports `review` (was `idle` pre-fix). Normal invocation still displays mode transition correctly.
  3. **Distribution gap.** All edits land in `~/audhd/bocek/...` (working tree). Slash commands invoke `~/.bocek/scripts/*` (installed copies) and `~/.bocek/primitives/*` + `~/.bocek/references/*` (symlinks to `~/.bocek/.repo/`). Working-tree edits don't affect slash-command behavior until either (a) commit + push + `bocek update`, or (b) manual `cp` of each edited file. Affects: scripts/preflight.sh, scripts/enforce-mode.sh, scripts/bocek, all 6 primitives, vault-format.md, context-format.md (new), contract-following.md. Not in any contract — user's call how to handle.
- **Discovery owed (write in /debugging next time it's the active seat):** preflight SIGPIPE bug. `scripts/preflight.sh` writes the mode file at line 312, near the END of the script. `set -euo pipefail` is on. When the caller pipes preflight output through `head` (or any truncating command), the first echo that hits the closed pipe receives SIGPIPE and `set -e` aborts the script before line 312 runs. Mode never gets written; the enforcement hook reads the prior mode. Observed in this session — invoked `bash ~/.bocek/scripts/preflight.sh implementation 2>&1 | head -8` and the mode file stayed "research". Worked around by `echo implementation > .bocek/mode`. Fix options: (a) write mode BEFORE any console output, (b) set `IFS=` and unset `pipefail` around the echo block, (c) document that preflight output cannot be piped through truncating commands.
